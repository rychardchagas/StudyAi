import { AgentsStatusClient } from "@/components/agents/AgentsStatusClient";

export const metadata = { title: "Agentes de IA — StudyAI" };

export default function AgentsPage() {
  return <AgentsStatusClient />;
}
