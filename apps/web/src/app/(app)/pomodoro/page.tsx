import { PomodoroClient } from "@/components/pomodoro/PomodoroClient";

export const metadata = { title: "Pomodoro — StudyAI" };

export default function PomodoroPage() {
  return <PomodoroClient />;
}
