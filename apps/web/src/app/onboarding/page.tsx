import { OnboardingClient } from "@/components/shared/OnboardingClient";

export const metadata = { title: "Começar — StudyAI" };

export default function OnboardingPage() {
  return <OnboardingClient />;
}
